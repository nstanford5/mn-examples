# What was actually verified in the generated UIs

Don't claim more than this when you reuse a pattern from one of these UIs.

**CI covers the rest for every UI.** It runs `yarn new:ui <name> --check`,
typecheck, unit tests and a production build (verification checklist steps
1–4 in `AGENTS.md`). The table below is only what CI can't run: the page in a
real browser, and a person approving deploys and calls in Lace.

The table is generated from each UI's `ui/verification.json`; don't edit it
here. After running a step, set its `status` (`verified`, `partial` or
`not-run`), `date` and `notes` (exactly what ran) in that file, then run
`yarn new:ui <name> --sync`. `--check` (and so CI) fails when the table is
stale, when a `verification.json` is malformed, or when one says the Lace
calls are verified while its `ui/README.md` still has the "TODO: end-to-end
verification with Lace" checklist. That checklist is how each UI's Lace steps
get run; delete it once every item is ticked.

<!-- BEGIN generated by `yarn new:ui` from examples/*/ui/verification.json; do not edit -->

| UI | In Chrome, no wallet (step 6) | Lace deploy (step 7) | Lace circuit calls (step 7) | preprod |
|---|---|---|---|---|
| battleship | ✅ 1 | — | — | — |
| calculator | ✅ 2 | — | — | — |
| hello-world | ✅ 3 | ◐ 4 | — | — |
| private-party | ✅ 5 | — | — | — |
| token-transfers | ✅ 6 | — | — | — |

✅ verified · ◐ partly verified (see its note) · — not run

1. 2026-09-24: The persistent store wrote a battleship private state to IndexedDB and read it back after a reload (`bigint`s and a byte-identical secret key). A wrong passphrase was refused with `WrongPassphraseError`. The constructor, `acceptGame` (with witnesses) and the pure `getDappPubKey` ran in the page. The vitest suite replays the Node test's game, both cheat attempts included.
2. 2026-09-24: `divide` ran in the page, calling the `divMod` witness. That covers the witness wiring only.
3. 2026-09-24: The page loaded, the WASM ledger and a circuit ran in it, and ZK assets were served as binaries.
4. 2026-09-24: A deploy tx was built and proven by the local proof server, and `yarn fund:wallet` got DUST to the Lace wallet. This predates the move of deploy/join/forget into `hooks/use-deployment.ts` and `components/deployment-card.tsx`; re-run it before calling that verified. The moved `yarn fund:wallet` (packages/fast-sync, one sponsor per wallet) was run twice against a throwaway seed wallet, not Lace: the wallet received 1,000 NIGHT and accruing DUST, and the rerun took the "already sponsored" path.
5. 2026-09-24: The constructor and `rsvp` ran in the page. `lib/addresses.ts` decoded a Bech32m unshielded address. A `{ secret }` private state survived a reload, and a wrong passphrase was refused. The vitest suite replays the Node test's party and checks, for every person and circuit at every step, that `actionError` predicts what the contract accepts. Unrun with Lace: `checkIn`'s NIGHT payment (the wallet must add it while balancing) and `claimFees`'s payout.
6. 2026-09-25: `mintAndReceive` and `mintAndSendShielded` ran in the page. `customTokenColor` matched the color the circuit returned, and a verifier key was served byte-for-byte. The vitest suite replays the Node test's sequence and checks every call's `Effects` (mints, inputs, outputs, coin claims), and that the coin book finds the coins the shielded circuits return. Unrun with Lace, and not covered by any other UI: the wallet supplying a custom-token or shielded-coin input while balancing (`receiveTokens`, `receiveShieldedTokens`), and `getUnshieldedBalances` / `getShieldedBalances` keys matching the hex colors `<WalletBalancesCard>` labels.

<!-- END generated -->

## Never run anywhere

- Wallet-delegated proving.
- A second browser profile joining.
- Re-joining after a reload through the passphrase card in the full app.

## Template pieces checked only by unit tests and CI builds

No real transaction has gone through these yet:
- the deploy-input slot and non-destructive re-join;
- generic circuit forms, including **Use my address** (`userAddress`), **Use
  my key** (`coinPublicKey`), the shielded-coin picker (`shieldedCoin`,
  `lib/coin-book.ts`) and the return-value line;
- `<WalletBalancesCard>`, including whether the connector's balance keys
  match the hex colors it labels;
- the funding hint's `mn_addr_` argument in `<DeploymentCard>`.
